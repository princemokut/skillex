/**
 * Message routes
 * Handles chat message operations
 */
import { FastifyInstance } from 'fastify';
/**
 * Register message routes
 * @param fastify Fastify instance
 */
export default function messageRoutes(fastify: FastifyInstance): Promise<void>;
