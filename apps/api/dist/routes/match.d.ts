/**
 * Match routes
 * Handles user matching operations
 */
import { FastifyInstance } from 'fastify';
/**
 * Register match routes
 * @param fastify Fastify instance
 */
export default function matchRoutes(fastify: FastifyInstance): Promise<void>;
