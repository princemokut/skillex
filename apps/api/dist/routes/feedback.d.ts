/**
 * Feedback routes
 * Handles feedback operations
 */
import { FastifyInstance } from 'fastify';
/**
 * Register feedback routes
 * @param fastify Fastify instance
 */
export default function feedbackRoutes(fastify: FastifyInstance): Promise<void>;
