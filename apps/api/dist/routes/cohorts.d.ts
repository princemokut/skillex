/**
 * Cohort routes
 * Handles cohort operations
 */
import { FastifyInstance } from 'fastify';
/**
 * Register cohort routes
 * @param fastify Fastify instance
 */
export default function cohortRoutes(fastify: FastifyInstance): Promise<void>;
