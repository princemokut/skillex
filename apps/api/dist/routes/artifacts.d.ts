/**
 * Artifact routes
 * Handles artifact operations
 */
import { FastifyInstance } from 'fastify';
/**
 * Register artifact routes
 * @param fastify Fastify instance
 */
export default function artifactRoutes(fastify: FastifyInstance): Promise<void>;
