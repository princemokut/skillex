/**
 * Referral routes
 * Handles referral operations
 */
import { FastifyInstance } from 'fastify';
/**
 * Register referral routes
 * @param fastify Fastify instance
 */
export default function referralRoutes(fastify: FastifyInstance): Promise<void>;
