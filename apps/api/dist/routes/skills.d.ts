/**
 * Skills routes
 * Handles user skills operations
 */
import { FastifyInstance } from 'fastify';
/**
 * Register skills routes
 * @param fastify Fastify instance
 */
export default function skillRoutes(fastify: FastifyInstance): Promise<void>;
