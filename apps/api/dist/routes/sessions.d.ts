/**
 * Session routes
 * Handles session operations
 */
import { FastifyInstance } from 'fastify';
/**
 * Register session routes
 * @param fastify Fastify instance
 */
export default function sessionRoutes(fastify: FastifyInstance): Promise<void>;
